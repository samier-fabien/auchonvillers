<form method="post" action="{{ path('votes_delete', {'locale': app.request.locale, 'id': vote.id}) }}" onsubmit="return confirm('{% trans %}Etes-vous sure de vouloir supprimer cet élément ?{% endtrans %}');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vote.id) }}">
    <button class="btn btn-primary">{% trans %}Supprimer{% endtrans %}</button>
</form>
