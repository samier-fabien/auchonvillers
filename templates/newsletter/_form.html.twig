    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
        {{ form_row(form.new_content_fr) }}
        {{ form_row(form.new_content_en) }}
        {{ form_errors(form) }}
        <button type="submit" class="btn btn-primary">{{ button_label|trans }}</button>
    {{ form_end(form) }}